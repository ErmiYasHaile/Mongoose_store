<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../css/style.css">
    <title>Show Page</title>
</head>
<body>
    
<div class="containershow">
  <nav>
    <a href="/items">
      <button>Back</button>
    </a>
</nav>
  <div class="cardshow">
    <h1><%=product.name %> </h1>

    <div class="imgs"> 
    <img src=<%=product.img %>> <br>
    </div>
    <br><%=product.description %></br>
    Price:$ <%=product.price %> </br>
    <%=product.qty %> remaining </br>

    <div class="editbtn">
    <a href="/items/<%= product._id%>/edit"> <button>Edit this item</button></a> 
  </div>
  <div class="deletebtn">
    <form action="/items/<%= product._id%>?_method=DELETE" method="post">
        <!-- <input type="submit" value="DELETE"> -->
       <br> <button>Delete</button>
    </form>
  </div>
    <% if (product.qty > 0) { %>
        <form action="/items/<%= product._id %>?_method=PUT" method="post">
          <input type="hidden" name="qty" value="<%= product.qty - 1 %>">
          <br><button>BUY</button>
        </form>
        <br>
      <% } else { %>
        <br><button class="stockbtn"> OUT OF STOCK </button>
      <% } %>
    </div>
  </div>
</body>
</html>

